# Alpaca API credentials (paper trading) — https://app.alpaca.markets
ALPACA_API_KEY=your_alpaca_key_here
ALPACA_SECRET_KEY=your_alpaca_secret_here

# Optional: Claude-scored news sentiment (keyword heuristic without it)
ANTHROPIC_API_KEY=
SENTIMENT_MODEL=claude-opus-4-8

# Optional: LLM strategy analyst (cloud Ollama, OpenAI-compatible API)
# Toggle on/off from the dashboard — off by default.
# For Ollama Cloud: base URL = https://ollama.com/v1, get API key at
# https://ollama.com/settings/keys
ANALYST_OLLAMA_BASE_URL=
ANALYST_OLLAMA_API_KEY=
ANALYST_OLLAMA_MODEL=deepseek-r1
ANALYST_TRADE_REVIEW_INTERVAL_HOURS=4
ANALYST_TRADE_LOOKBACK=50

# Trading configuration — only TRADING_SYMBOLS and API_PORT remain env vars.
# Everything else (position size, max positions, cooldown, etc.) is now
# tunable from the dashboard's Settings → Operational Environment card.
TRADING_SYMBOLS=ALL
BACKEND_API_URL=http://trading_backend:8000

# Nightly optimizer (equity engine only). Runtime scales linearly with
# LOOKBACK_DAYS × MAX_SYMBOLS: the measured baseline is ~108 min at 30 × 10,
# so the 60 × 15 defaults land near 5.5 h — keep the product inside the
# 22:00 UTC → 08:00 UTC overnight window.
OPTIMIZER_LOOKBACK_DAYS=60
OPTIMIZER_MAX_SYMBOLS=15
OPTIMIZER_TRAIN_FRACTION=0.75
# Holdout is cut into this many sequential folds; a winner must be
# profitable in a majority of them and in aggregate.
OPTIMIZER_VALIDATION_FOLDS=3
# Friction floor/fallback (percent). Stop slippage is normally calibrated
# from realized stop fills each run; this value is the optimistic floor and
# the fallback when fewer than OPTIMIZER_CALIBRATION_MIN_SAMPLES stop fills
# are on record.
OPTIMIZER_COST_PCT=0.10
OPTIMIZER_STOP_SLIP_PCT=0.05
OPTIMIZER_CALIBRATION_MIN_SAMPLES=8
